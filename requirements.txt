# Core PyTorch dependencies (install first)
torch==2.5.1
torchvision==0.20.1

# Basic dependencies
numpy>=1.23.5,<2
opencv-python==4.10.0.84
tqdm
imageio
imageio-ffmpeg
easydict
ftfy

# ML/AI libraries
diffusers>=0.31.0
transformers==4.49.0
tokenizers==0.21.1
accelerate==1.6.0
dashscope

# Specialized dependencies (install after PyTorch)
# flash_attn  # Install separately due to compilation requirements
# xfuser      # Install separately due to potential conflicts
